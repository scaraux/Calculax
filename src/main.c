#include <stdio.h>
#include "interpreter.h"

int main(int ac, char **av)
{
    // t_hist *head = NULL;

    // insert_element_to_history(&head, "toto");
    // insert_element_to_history(&head, "titi");
    // // insert_element_to_history(&head, "tata");
    // // insert_element_to_history(&head, "tutu");
    // // insert_element_to_history(&head, "t222");


    // while (head != NULL)
    // {
    //     printf("%s\n", head->line);
    //     head = head->next;
    // }
    return calculax();
}